@PACKAGE_INIT@

set(dgm-lib_FOUND TRUE)
set(dgm-lib_VERSION @PROJECT_VERSION@)
set(dgm-lib_INCLUDE_DIRS "@CMAKE_CURRENT_LIST_DIR@/../include")

if(NOT TARGET dgm::dgm-lib)
    add_library(dgm::dgm-lib ALIAS dgm-lib)
endif()
